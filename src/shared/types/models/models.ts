export interface IUser {
    id: number;
    title: string;
}

export interface IJob {
    title: string;
    id: number;
}

export interface IProfileData {
    name: string;
    contentToken: string;
}
